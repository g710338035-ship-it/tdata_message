php think telegram:task-manager-http start 启动任务管理器
php think telegram:task-manager stop 停止所有任务和管理器
php think telegram:task-manager restart 重启管理器
php think telegram:task-manager status 查看运行状态

# 强制终止所有telegram:task-manager相关进程
kill -9 $(ps -ef | grep "telegram:task-manager" | grep -v grep | awk '{print $2}')

# 清理PID文件（防止启动时提示已运行）
rm -f runtime/telegram_task_manager.pid


查看状态：php think telegram:task-manager --action status
启动：php think telegram:task-manager --action start
停止：php think telegram:task-manager --action stop
重启：php think telegram:task-manager --action restart

# 查找所有相关工作进程
ps -ef | grep telegram:task-manager | grep -v grep

# 强制杀死所有相关进程（替换为实际查找到的PID）
kill -9 $(ps -ef | grep telegram:task-manager | grep -v grep | awk '{print $2}')

kill -9 $(ps -ef | grep telegram:task-manager-http | grep -v grep | awk '{print $2}')

# 先停止旧进程（参考之前的停止方法）
ps aux | grep "think queue:work --queue mtuser_handle" | grep -v grep | awk '{print $2}' | xargs kill
# 启动新进程（前台测试，查看实时日志）
php think queue:work --queue mtuser_handle --verbose


php think queue:work --queue telegram --verbose

ps -ef | grep mtuser_handle | grep -v grep

php ChatWorker.php start -d
./chat_service.sh stop  # 停止服务（含强制终止）
./chat_service.sh start # 启动服务


php ChatWorker.php start

# 批量杀死包含WorkerMan关键字的进程
ps -ef | grep -i 'WorkerMan' | grep -v 'grep' | awk '{print $2}' | xargs kill -9


python account_checker.py batch